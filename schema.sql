DROP TABLE IF EXISTS moviTable;

CREATE TABLE IF NOT EXISTS moviTable (
    id varchar(255),
    title varchar(255),
  poster_path varchar(255),
     release_date varchar(255) ,
    overview varchar(65000),
    myPersonal_Commen varchar(65000)
);